spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: 'TRUE'
    hibernate:
      ddl-auto: update
    open-in-view: 'false'
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: Root123456
    url: jdbc:mysql://localhost:3306/freelancer_platform2?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC
  mail:
    host: smtp.abv.bg
    port: 465
    username: kros4o@abv.bg
    password: messi970322
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false
    default-encoding: UTF-8
logging:
  level:
    org:
      hibernate:
        type:
          descriptor: TRACE
        SQL: DEBUG
      nodeValue: WARN
    blog: WARN
server:
  port: 8080
  error:
    whitelabel:
      enabled: false
#offers:
#  api:
#    baseUrl: "${OFFERS_BASE_URL:http://localhost:8081}"